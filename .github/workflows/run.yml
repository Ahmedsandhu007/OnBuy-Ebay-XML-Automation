name: Generate XML Feed

permissions:
  contents: write

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

concurrency:
  group: xml-feed
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # IMPORTANT: required for git pull/rebase

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Save credentials
        run: |
          printf '%s' '${{ secrets.GOOGLE_CREDS }}' > credentials.json

      - name: Set credentials env
        run: echo "GOOGLE_APPLICATION_CREDENTIALS=credentials.json" >> $GITHUB_ENV

      - name: Run script
        run: python generate_xml.py

      - name: Commit and Push XML (safe)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add feed.xml

          # Commit only if changes exist
          git diff --cached --quiet || git commit -m "Update XML"

          # Rebase with latest remote changes
          git pull --rebase origin main

          # Push safely
          git push origin main
